#include "holberton.h"
include "main.h"

/**
 * main- computes the absolute value of an integer
 * 
 * Return: returns
 */

int _abs(int n)
{
  if (n > 0)
    return (n);
  else
    return (-1 * n);
}
